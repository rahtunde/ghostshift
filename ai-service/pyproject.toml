[tool.ruff]
target-version = "py312"
line-length = 88
select = ["E", "F", "I", "B", "UP"]
ignore = ["B008"]

[tool.ruff.isort]
known-first-party = ["routers", "schemas", "services"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
